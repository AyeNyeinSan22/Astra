# Python dependencies for the smart bin edge components.
# Install with:  pip install -r requirements.txt

# Vision detector (vision_detector.py) – webcam inference on PC
opencv-python
tflite-runtime   # lightweight TFLite inference; use tensorflow if unavailable

# Firebase simulation (simulate_firebase.py)
requests
